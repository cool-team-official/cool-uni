import $ from "./static/css/common.scss";

// 颜色相关
export const color = {
    primary: $.primaryColor || '#409eff',
    success: $.successColor || '#67c23a',
    warning: $.warningColor || '#e6a23c',
    error: $.errorColor || '#dd524d',
    info: $.infoColor || '#909399',
    disabled: $.disabledColor || '#c0c4cc',
}
